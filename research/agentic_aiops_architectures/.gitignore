# Credentials (API keys, passwords — never commit)
config/.env

# Local MLflow file-store leftovers (tracking uses OpenShift Route)
/mlartifacts/
/mlruns/
mlflow.db

# Scratch / working notes (may contain debug output with credentials)
/scratch/

# Python
__pycache__/
*.pyc
*.pyo

# IDE
.vscode/
.idea/
